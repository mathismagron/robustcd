# GPU training stack for Alliance clusters, pinned to wheels listed by
# `avail_wheels` on Vulcan (2026-09-25, python 3.11). Install with
#   pip install --no-index -r requirements-gpu.txt
# Pinned so that every model in the benchmark trains on the same stack;
# bump deliberately and record the change.
torch==2.14.0
torchvision==0.29.0
triton==3.6.0
mamba_ssm==2.2.4
causal_conv1d==1.5.4
timm==1.0.29
einops==0.8.2
numpy
scipy
pillow
